# EverTrace managed user unit v1
[Unit]
Description=EverTrace local daemon

[Service]
Type=simple
ExecStart=@DAEMON@ --config @CONFIG@
UMask=0077
Restart=on-failure
RestartSec=2s
TimeoutStopSec=10s

[Install]
WantedBy=default.target
